name: CI

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout project
        uses: actions/checkout@v4

      - name: Run tests in Docker
        run: |
          docker run --privileged \
            -v $(pwd):/mnt \
            lolopopikop/kubsh_test:local /bin/bash -c "
              echo '=== Копируем check.sh из /opt ==='
              
              # Копируем check.sh из /opt
              cp /opt/check.sh /mnt/check.sh
              chmod +x /mnt/check.sh
              
              echo 'Содержимое check.sh:'
              head -20 /mnt/check.sh
              
              # Запускаем тесты
              cd /mnt
              echo '=== Запускаем check.sh ==='
              ./check.sh
              
              # Проверяем что .deb создался
              if [ -f kubsh.deb ]; then
                echo '✅ .deb пакет создан: kubsh.deb'
                ls -la kubsh.deb
              else
                echo '⚠️ .deb пакет НЕ создан!'
                echo 'Проверяем что есть в папке:'
                ls -la
              fi
            "

      - name: Collect deb artifact
        run: |
          echo "Ищем .deb файл..."
          find . -name "*.deb" 2>/dev/null || echo ".deb файлов не найдено"
          
          if [ -f kubsh.deb ]; then
            echo "Found kubsh.deb"
          else
            # Ищем ЛЮБОЙ .deb файл
            find . -name "*.deb" -exec cp {} kubsh.deb \; 2>/dev/null || true
            
            if [ -f kubsh.deb ]; then
              echo "Скопирован найденный .deb файл"
            else
              echo "⚠️ .deb файл не найден"
              # Создаём пустой файл, чтобы CI не упал
              touch kubsh.deb
            fi
          fi

      - name: Upload DEB artifact
        uses: actions/upload-artifact@v4
        with:
          name: kubsh-deb
          path: kubsh.deb
